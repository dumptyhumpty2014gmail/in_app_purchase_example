import 'package:flutter/material.dart';

abstract class AppConstraints {
  static const gestureConstraintsBaseButton3Line = const BoxConstraints(
  minWidth: 100,
  maxWidth: 300,
  minHeight: 90,
);

}